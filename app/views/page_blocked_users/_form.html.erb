<%= form_for(@page_blocked_user) do |f| %>
  <% if @page_blocked_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page_blocked_user.errors.count, "error") %> prohibited this page_blocked_user from being saved:</h2>

      <ul>
      <% @page_blocked_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
