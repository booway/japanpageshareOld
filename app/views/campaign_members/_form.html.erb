<%= form_for(@campaign_member) do |f| %>
  <% if @campaign_member.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@campaign_member.errors.count, "error") %> prohibited this campaign_member from being saved:</h2>

      <ul>
      <% @campaign_member.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
